import { type FC } from "react";

export const meta = () => {
  return {
    title: "Attend Event",
    description: "Attend Event",
  };
};

const PageComponent: FC = () => {
  return <div>Attend Event TODO: use code</div>;
};

export default PageComponent;
