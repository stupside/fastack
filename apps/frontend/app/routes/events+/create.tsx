import { type FC } from "react";

export const meta = () => {
  return {
    title: "Create Event",
    description: "Create Event",
  };
};

const PageComponent: FC = () => {
  return (
    <div>
      <h1>Create Event</h1>
    </div>
  );
};

export default PageComponent;
